{
  "name": "consumer-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js --verbose -o bundle.js",
    "watch": "watchify index.js --verbose -o bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify": "^15.0.0",
    "client-oauth2": "^4.1.0",
    "watchify": "^3.9.0"
  }
}
